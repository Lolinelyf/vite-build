# Vite Build

Этот проект — современный шаблон для быстрой и удобной сборки сайтов на базе [Vite](https://vitejs.dev/).

## 🚀 Особенности

- **Молниеносная разработка**: Vite обеспечивает быстрый запуск и HMR
- **Многостраничность**: Поддержка нескольких HTML-страниц
- **SCSS/SASS**: Поддержка препроцессоров
- **Оптимизированная сборка**: Автоматическая оптимизация и разделение кода
- **Раздельная сборка скриптов**: Главный скрипт собирается как IIFE (`build/assets/js/main.js`), отдельные скрипты из папки `separate` — как отдельные файлы с сохранением структуры
- **Минификация отдельных скриптов**: Все файлы из `src/assets/js/separate` минифицируются и копируются в билд
- **Постобработка HTML и скриптов**: После сборки автоматически убирается `type="module"`, заменяются preload-ссылки на `<script>`, подключаются отдельные скрипты и корректируются пути
- **Автоматизация билда**: Один npm-скрипт (`npm run build`) полностью собирает проект, включая IIFE-версию, минификацию отдельных файлов и постобработку
- **Удаление временных файлов**: в процессе минимизации и IIFE-билда все скрипты заменяются на традиционный скрипт
- **Линтинг CSS/SCSS**: Stylelint
- **(Рекомендуется) Линтинг JS**: ESLint (можно добавить)

## 🛠️ Технологии

- **Vite** — основной сборщик
- **SASS/SCSS** — стили
- **Stylelint** — линтинг стилей
- **PostCSS** — обработка CSS
- **Node.js** — скрипты автоматизации

## 📁 Структура проекта

```
├── src/
│   └── assets/
│       ├── js/
│       │   ├── main.js                # Главный JS (собирается как IIFE, результат — build/assets/js/index.js)
│       │   └── separate/              # Отдельные JS-файлы (каждый — отдельный скрипт, минифицируются и копируются в билд)
│       └── styles/                    # SCSS/SASS
├── public/                            # Статика (картинки, шрифты, сторонние css)
├── build/
│   └── assets/
│       └── js/
│           ├── main.js                # Главный скрипт сайта (IIFE, итоговый)
│           └── separate/              # Все отдельные минифицированные скрипты
├── vite.config.js                     # Основная конфигурация Vite (многоточечная, partials)
├── vite.iife.config.js                # Конфиг для отдельной сборки IIFE (главный скрипт)
├── scripts/minify-separate.cjs        # Минификация и копирование отдельных скриптов
├── scripts/postbuild-fix-scripts.cjs  # Скрипт постобработки HTML и подмена main.js
├── scripts/prebuild-clean-build.cjs   # Очистка билда перед сборкой
├── package.json                       # Скрипты и зависимости
└── ...
```

## ⚡ Быстрый старт

1. Установите зависимости:

```bash
npm install
```

2. Запустите dev-сервер:

```bash
npm run dev
```

3. Соберите проект (выполняются все этапы: основной билд, сборка IIFE, минификация отдельных скриптов, постобработка):

```bash
npm run build
```

4. Для предпросмотра используйте:

```bash
npm run preview
```

## 🚦 Основные npm-скрипты

- `npm run build` — полный билд (очистка, vite build, vite.iife, минификация отдельных скриптов, postbuild)
- `npm run dev` — запуск dev-сервера
- `npm run vite:iife` — ручная сборка главного скрипта как IIFE (обычно не требуется запускать отдельно)
- `npm run minify-separate` — минификация и копирование отдельных скриптов
- `npm run postbuild` — постобработка HTML и удаление main.js

## 📝 Особенности процесса билда

- Главный скрипт всегда лежит в `build/assets/js/main.js` и подключается на всех страницах
- Все отдельные скрипты из `src/assets/js/separate` автоматически минифицируются и копируются в `build/assets/js/separate`
- В HTML автоматически подключаются все необходимые отдельные скрипты
- Временный файл `main.js` заменяется после билда
- Все пути и подключения корректируются автоматически постобработчиком

- Перед сборкой автоматически очищается папка `build/`.
- После сборки главный скрипт (`main.js`) собирается как обычный (IIFE) и кладётся в `build/assets/js/main.js`.
- Все скрипты из `src/assets/js/separate/` собираются как отдельные обычные скрипты в `build/assets/js/separate/`.
- После сборки запускается постобработка HTML (убирает type="module", заменяет preload на `<script>`).

## 🧩 Основные npm-скрипты

- `npm run dev` — запуск dev-сервера
- `npm run build` — полноценная сборка (очистка + vite build + vite build IIFE + постобработка)
- `npm run lint:css` — линтинг SCSS/CSS

## 🧹 Автоматическая очистка build/

Перед каждой сборкой папка `build/` автоматически удаляется (используется пакет [`rimraf`](https://www.npmjs.com/package/rimraf)).

## 📝 Советы

- Для чистоты JS-кода рекомендуется добавить ESLint
- Для автоформатирования используйте Prettier
- Вся статика (картинки, шрифты) должна лежать в `public/`
- Для сложной постобработки HTML можно расширить скрипт `scripts/postbuild-fix-scripts.cjs`

---

**Если возникнут вопросы по настройке или сборке — смело обращайтесь!**

1. Установите зависимости:

```bash
npm install
```

2. Запустите сервер разработки:

```bash
npm run dev
```

3. Сборка для продакшена:

```bash
npm run build
```

4. Предпросмотр собранного проекта:

```bash
npm run preview
```

## 🔧 Конфигурация

### Vite

Проект настроен с учетом следующих особенностей:

- Автоматическое определение HTML файлов
- Разделение JS файлов на модули
- Оптимизированная структура выходных файлов
- Поддержка алиасов для удобного импорта
- Настройка обработки различных типов ресурсов

### Компоненты

- Компоненты реализованы через Handlebars
- Поддержка частичных шаблонов
- Возможность передачи данных в компоненты
- Горячая перезагрузка при изменении компонентов

### Стили

- Поддержка SCSS
- Автоматическая обработка через PostCSS
- Линтинг через Stylelint
- Оптимизация при сборке

### JavaScript

- Модульная структура
- Поддержка ES6+
- Линтинг через ESLint
- Оптимизация при сборке

## 📝 Процесс разработки

1. **Создание новой страницы**:

   - Создайте HTML файл
   - Используйте компоненты через Handlebars
   - Подключите необходимые скрипты и стили

2. **Добавление компонентов**:

   - Создайте `.hbs` файл в `src/components`
   - Используйте синтаксис Handlebars
   - Подключите компонент через `{{> _componentName }}`

3. **Работа со стилями**:

   - Используйте SCSS для стилизации
   - Следуйте правилам Stylelint
   - Используйте импорты для модульности

4. **JavaScript модули**:
   - Используйте ES модули
   - Следуйте правилам ESLint
   - Разделяйте код на логические модули

## 🧪 Тестирование

Проект включает следующие проверки:

- Линтинг JavaScript (ESLint)
- Линтинг CSS/SCSS (Stylelint)
- Проверка типов (если используется TypeScript)

## 📦 Сборка

При сборке проекта:

1. Обработка HTML файлов
2. Компиляция SCSS
3. Оптимизация JavaScript
4. Обработка статических файлов
5. Создание оптимизированной структуры

## 📝 Лицензия

MIT
